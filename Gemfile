source 'https://rubygems.org'

gem 'bundler'
gem 'grape'
gem 'lotusrb',      '0.2.0'
gem 'lotus-form', path: 'lib/lotus-form', require: 'lotus/form'
gem 'lotus-model', '~> 0.2', path: '../../vendor/lotus/model'
gem 'lotus-utils', '~> 0.2', path: '../../vendor/lotus/utils'
gem 'pg'
gem 'pry-byebug'
gem 'rake'

group :test do
  gem 'minitest'
  gem 'capybara'
end

group :production do
  # gem 'puma'
end
